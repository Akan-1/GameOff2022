using UnityEditor;

[CustomEditor(typeof(PlayerController2d))]
public class PlayerControllerEdiot : Editor
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
